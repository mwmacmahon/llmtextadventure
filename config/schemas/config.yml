Config:
  type: object
  properties:
    llm_config:
      type: LLMConfig
      required: true
    transformation_config:
      type: TransformationConfig
      required: true
    parsing_config:
      type: ParsingConfig
      required: true
# Additional parameters are only in subclasses
