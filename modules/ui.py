# Handles the interface of the game - CLI vs GUI vs API,
# receiving input from the user and displaying results, etc.


# Initialize console logging
import logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)